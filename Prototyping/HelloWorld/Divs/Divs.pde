/* Divs 20 Rectangles
*/
//
println(displayWidth, displayHeight);
fullScreen();
int appWidth = displayWidth;
int appHeight = displayHeight;
//
int paperWidth = 192;
int paperHeight = 168;
float DivX = appWidth * ??? / paperWidth; 
float DivY = appHeight * ??? / paperHeight; 
float DivWidth = appWidth * ??? / paperWidth;  
float DivHeight = appHeight * ??? / paperHeight; 
//
//rect( SongTitleDivX, SongTitleDivY, SongTitleDivWidth, SongTitleDivHeight );
float DivX = appWidth * 50 / paperWidth; 
float DivY = appHeight * 144 / paperHeight; 
float DivWidth = appWidth * 150 / paperWidth;  
float DivHeight = appHeight * 117 / paperHeight;
//
//rect( PlayButtonDivX, PlayButtonDivY, PlayButtonDivWidth, PlayButtonDivHeight );
float DivX = appWidth * 150 / paperWidth; 
float DivY = appHeight * 44 / paperHeight; 
float DivWidth = appWidth * 160 / paperWidth;  
float DivHeight = appHeight * 53 / paperHeight; 
//
//rect( PauseButtonDivX, PauseButtonDivY, PauseButtonDivWidth, PauseButtonDivHeight );
float DivX = appWidth * 71 / paperWidth; 
float DivY = appHeight * 15 / paperHeight; 
float DivWidth = appWidth * 85 / paperWidth;  
float DivHeight = appHeight * 25 / paperHeight; 
//
//rect( SkipTenDivX, SkipTenDivY, SkipTenDivWidth, SkipTenDivHeight );
float DivX = appWidth * 85 / paperWidth; 
float DivY = appHeight * 15 / paperHeight; 
float DivWidth = appWidth * 97 / paperWidth;  
float DivHeight = appHeight * 25 / paperHeight; 
//
//rect( BackTenDivX, BackTenDivY, BackTenDivWidth, BackTenDivHeight );
float DivX = appWidth * 61 / paperWidth; 
float DivY = appHeight * 15 / paperHeight; 
float DivWidth = appWidth * 71 / paperWidth;  
float DivHeight = appHeight * 25 / paperHeight; 
//
//rect( SkipSongDivX, SkipSongDivY, SkipSongDivWidth, SkipSongDivHeight );
float DivX = appWidth * 97 / paperWidth; 
float DivY = appHeight * 15 / paperHeight; 
float DivWidth = appWidth * 110 / paperWidth;  
float DivHeight = appHeight * 25 / paperHeight; 
//
//rect( PrevSongDivX, PrevSongDivY, PrevSongDivWidth, PrevSongDivHeight );
float DivX = appWidth * 49 / paperWidth; 
float DivY = appHeight * 15 / paperHeight; 
float DivWidth = appWidth * 61 / paperWidth;  
float DivHeight = appHeight * 25 / paperHeight; 
//
//rect( UnmuteDivX, UnmuteDivY, UnmuteDivWidth, UnmuteDivHeight );
float DivX = appWidth * 64 / paperWidth; 
float DivY = appHeight * 4 / paperHeight; 
float DivWidth = appWidth * 72 / paperWidth;  
float DivHeight = appHeight * 17 / paperHeight; 
//
//rect( MuteDivX, MuteDivY, MuteDivWidth, MuteDivHeight );
float DivX = appWidth * 72 / paperWidth; 
float DivY = appHeight * 4 / paperHeight; 
float DivWidth = appWidth * 80 / paperWidth;  
float DivHeight = appHeight * 17 / paperHeight; 
//
//rect( ShuffleDivX,  ShuffleDivY,  ShuffleDivWidth,  ShuffleDivHeight );
float DivX = appWidth * 80 / paperWidth; 
float DivY = appHeight * 4 / paperHeight; 
float DivWidth = appWidth * 90 / paperWidth;  
float DivHeight = appHeight * 17 / paperHeight; 
//
//rect( NoScreenDivX, NoScreenDivY, NoScreenDivWidth, NoScreenDivHeight );
float DivX = appWidth * 90 / paperWidth; 
float DivY = appHeight * 4 / paperHeight; 
float DivWidth = appWidth * 100 / paperWidth;  
float DivHeight = appHeight * 17 / paperHeight; 
//
//rect( DoubleSpeedDivX, DoubleSpeedDivY, DoubleSpeedDivWidth, DoubleSpeedDivHeight );
float DivX = appWidth * 150 / paperWidth; 
float DivY = appHeight * 38 / paperHeight; 
float DivWidth = appWidth * 160 / paperWidth;  
float DivHeight = appHeight * 44 / paperHeight; 
//
//rect( PlaylistDivX, PlaylistDivY, PlaylistDivWidth, PlaylistDivHeight );
float DivX = appWidth * 150 / paperWidth; 
float DivY = appHeight * 30 / paperHeight; 
float DivWidth = appWidth * 160 / paperWidth;  
float DivHeight = appHeight * 38 / paperHeight; 
//
//rect( ExitButtonDivX, ExitButtonDivY, ExitButtonDivWidth, ExitButtonDivHeight );
float DivX = appWidth * 190 / paperWidth; 
float DivY = appHeight * 162 / paperHeight; 
float DivWidth = appWidth * 197 / paperWidth;  
float DivHeight = appHeight * 168 / paperHeight; 
//
//rect( ScreenDivX, ScreenDivY, ScreenDivWidth, ScreenDivHeight );
float DivX = appWidth * ??? / paperWidth; 
float DivY = appHeight * ??? / paperHeight; 
float DivWidth = appWidth * ??? / paperWidth;  
float DivHeight = appHeight * ??? / paperHeight; 
//
//rect( SongBarDivX, SongBarDivY, SongBarDivWidth, SongBarDivHeight );
float DivX = appWidth * ??? / paperWidth; 
float DivY = appHeight * ??? / paperHeight; 
float DivWidth = appWidth * ??? / paperWidth;  
float DivHeight = appHeight * ??? / paperHeight; 
//
